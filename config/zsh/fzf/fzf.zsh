# Setup fzf

# Auto-completion
# ---------------
[[ $- == *i* ]] && source "$HOME/.config/zsh/fzf/completion.zsh" 2> /dev/null

# Key bindings
# ------------
source "$HOME/.config/zsh/fzf/key-bindings.zsh"
